(() => {
  class ImportActions {

    // --------------------------------------------------
    // Setup
    // --------------------------------------------------
    constructor() {
      this.generateActions(
        'storeConference',
      );
    }
  }
  this.ImportActions = alt.createActions(ImportActions);
})();
